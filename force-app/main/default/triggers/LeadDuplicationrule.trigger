trigger LeadDuplicationrule on Lead (after insert) {
  /*  if(ContactEmailSend.runOnce()){
        List<Lead> leadList = new List<Lead>();
        leadList = Trigger.new.deepClone();
        insert leadList;
    }
*/
}